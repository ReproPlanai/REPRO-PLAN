# REPRO PLAN Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Application Environment
NODE_ENV=development
PORT=5000

# Database Configuration (PostgreSQL)
# Option 1: Use individual variables (recommended for local development)
DB_HOST=your-db-host.db.ondigitalocean.com
DB_PORT=5432
DB_NAME=reproplan
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here

# Option 2: Use connection string (alternative method)
# DATABASE_URL=postgresql://doadmin:your_password@your-db-host.db.ondigitalocean.com:25060/defaultdb?sslmode=require

# CORS Configuration
CORS_ORIGIN=https://your-frontend-app.netlify.app

# Rate Limiting
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# JWT Secret (for future authentication features)
JWT_SECRET=your_jwt_secret_key_here

