# REPRO PLAN Backend Environment Variables for DigitalOcean
# Set these in DigitalOcean App Platform Environment Variables section

# Application Environment
NODE_ENV=production
PORT=5000

# Database Configuration (PostgreSQL)
# DigitalOcean provides DATABASE_URL automatically when you add a Managed Database
# Format: postgresql://user:password@host:port/database?sslmode=require
# You can also use individual variables if preferred:
# DB_HOST=your-db-host.db.ondigitalocean.com
# DB_PORT=25060
# DB_NAME=reproplan
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password

# CORS Configuration
# Update with your frontend URL (e.g., https://repro-plan.netlify.app)
CORS_ORIGIN=https://your-frontend-domain.com

# Rate Limiting
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# JWT Secret (generate a strong random string)
# Use: openssl rand -base64 32
JWT_SECRET=your_strong_jwt_secret_key_here

# Optional: Logging
LOG_LEVEL=info

